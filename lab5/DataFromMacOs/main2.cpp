#include <iostream>
#include <fstream>
#include <vector>
using namespace std;

int main(int argc, const char * argv[]) {
    int countOut;
    cin >> countOut;
    
    int count = 0;
    
    while (count < countOut){
        count++;
        malloc(100);
    }
    return 0;
}
